{
  "author": "Turo engineering",
  "description": "GitHub Actions for Releases",
  "devDependencies": {
    "lerna": "8.2.4"
  },
  "engines": {
    "node": ">= 20.0.0"
  },
  "workspaces": [
    "semantic-release"
  ],
  "license": "MIT",
  "name": "@open-turo/actions-release",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "prepare": "npm run lint && npm run build",
    "test": "lerna run test --"
  }
}
